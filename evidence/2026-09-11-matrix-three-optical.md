# Matrix optical inspection — 2026-09-11

Classification: sanitized immutable observation summary. No live mutation.
Private source: ignored `inbox/optical-20260911/`, files named in the
[packet](../implementation/2026-09-11-matrix-three-optical.packet.md).

## Confirmed

- Strict management SSH reached hv-matrix; PVE 9.2.2, kernel 7.0.2-6-pve.
  VM310 `b70-encode` was running. Its guest-agent root inventory reports
  `b70-encode-matrix`; SMBIOS UUID matches PVE configuration exactly.
- Complete VM configuration remained identical across two captures. Both
  `hostpci0=0000:23:00.0,pcie=1` and `hostpci1=0000:24:00.0,pcie=1`
  remained present. Guest Battlemage uses xe; audio uses snd_hda_intel.
- All three intended physical optical identities are unique in the capture.
  Serial values and complete by-id links are retained privately.
- SATA HP GUD1N is host sr0/sg2 on Intel `00:17.0`, group 30 (one member),
  ahci. That controller also owns both Patriot rpool member disks. Disk serials
  match the zpool status paths. No per-function reset file exists. Host rpool
  is ONLINE with no known data errors. This is a forbidden assignment target.
- SATA LG WH16NS60 is host sr1/sg4 on ASMedia `03:00.0`, group 36 (one
  member), ahci. Its only block descendant is the optical drive. Sysfs reset
  exists and advertises `pm bus`. Two ATA ports are represented. No reset was
  attempted; physical spare-port availability remains unverified.
- USB BP50NB40 has host topology `1-13`, VID:PID `152e:2571`. Exactly one
  guest USB device matches its serial. Existing `usb0: host=1-13,usb3=1`
  is persistent port-bound assignment, not serial-bound or vendor/product-bound.
  The host has no sr node for the USB drive while QEMU owns it.
- Guest sr1/sg3 is the physical USB BP50NB40. Guest sr0/sg2 is QEMU DVD-ROM,
  generated by the existing stable-by-id ide2 mapping to the LG drive. HP is
  absent. Only one native physical optical drive is presently proven in guest.
- Root-visible fuser shows no guest optical handles and no host HP handles.
  Host LG sr1 is held by the running VM's QEMU PID, consistent with ide2.
  No optical mount is reported by findmnt. No ffmpeg/MakeMKV/HandBrake process
  was observed. Application queues and complete workload readiness were not
  qualified for shutdown because the hardware gate already failed.
- Management remains vmbr0/192.168.10.22 with its existing default route.
  PBS storage was unreachable during orientation; no backup or storage repair
  was attempted and no current backup acceptance is claimed.

## Distinctions and limits

The operator reports recent SATA-controller installation/relocation. Current
topology confirms the LG drive on the dedicated ASMedia controller, not when
it was installed. Historical sr/sg numbers were not used as selectors.
IOMMU membership and reset advertisement are read-only capability evidence,
not proof of a successful VFIO reset or repeated VM start.

No shutdown, restart, hostpci/USB/ide change, disc mount/read/write/rip, Docker
change, ARM deployment or media mutation occurred. The guest remained running.
Reboot persistence of a new three-drive configuration is not tested because
no such configuration was applied. Existing GPU driver presence is proven;
no production encode or GPU operational regression test was performed.

## Outcome

Desired state recorded but not realized. The HP controller's rpool membership
triggers the operator's stop condition before mutation. A safe HP attachment
decision is required; whole onboard-controller assignment is excluded. See
the [desired state](../docs/MATRIX_OPTICAL_DESIRED_STATE.md) and
[runbook](../runbooks/2026-09-11-matrix-three-optical.md) for the bounded handoff.
