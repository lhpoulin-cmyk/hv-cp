# Firewall implementation packets

Use one dated packet for one node and one intended flow. Copy
`../templates/FIREWALL-CHANGE-PACKET.md`; never edit the template into a live
record.

The first planned packet is Lore accepting SSH from Hadrian's authority
identity. It remains blocked until Hadrian's VLAN-80 attachment, address,
credential, and direct client path are accepted independently.

A draft is not authority. Execution requires a fresh before state, exact diff,
independent recovery path, operator approval, validation, rollback, and
evidence publication.
