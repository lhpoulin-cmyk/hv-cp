# Implementation packets

Create one dated packet per authorized live change. The packet must name the
target, mutation, rollback, and evidence destination before execution. Keep
the result with the packet after validation; do not replace earlier packets.

Use `../templates/NODE_CONTROL_PLANE_CHECKLIST.md` as the starting checklist
and `../runbooks/CONTROLLED_CHANGE.md` as the execution discipline.
