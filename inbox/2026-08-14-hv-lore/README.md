# hv-lore raw capture set — 2026-08-14

These are immutable, read-only command outputs collected from `hv-lore` on
2026-08-14 and relocated from the transaction workspace into the governed
`hv-cp` recovered-artifact inbox.

The companion `SHA256SUMS.txt` records the hashes of every raw text capture.
Do not treat these outputs as authorization to repeat a command or change the
host. No disk, pool, partition table, bootloader, or firmware state was
modified by the collection documented here.
